<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link rel="stylesheet" href="/Content/bootstrap.min.css"/>
    <script type="text/javascript" src="/Scripts/angular.min.js"></script>
    <script type="text/javascript" src="/Scripts/App/app.js"></script>
    <script type="text/javascript" src="/Scripts/App/DashboardController.js"></script>
</head>
<body ng-app="ConversionApp">
    <div ng-controller="DashboardCtrl">
        <div class="container">
            <div class="h3">
                Enter Amount And Your Name
            </div>
            
            <div class="row">
                <div class="col-md-6">
                    <div class="form-horizontal">
                        <div class="form-group">
                            Name: <input type="text" class="form-control" ng-model="Name" />
                        </div>
                        <div class="form-group">
                            Amount: <input type="text" class="form-control" ng-model="Amount" />
                        </div>
                        <div class="form-group">
                            <input type="button" value="Convert" class="btn btn-default" ng-click="ConvertHandler()" />
                        </div>
                    </div>
                </div>
                <div class="col-md-6"></div>
            </div>
            <div class="row" ng-if="ResultName!=null">
                <div class="col-md-12">
                    <p>Name: {{ResultName}}</p>
                    <p>Amount in Text: {{ResultAmount}}</p>
                </div>
                {{ErrorMessage}}
            </div>
        </div>
    </div>
</body>
</html>
